import { FontsizeEstiloDirective } from './fontsize-estilo.directive';

describe('FontsizeEstiloDirective', () => {
  it('should create an instance', () => {
    const directive = new FontsizeEstiloDirective();
    expect(directive).toBeTruthy();
  });
});
