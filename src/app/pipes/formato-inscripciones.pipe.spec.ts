import { FormatoInscripcionesPipe } from './formato-inscripciones.pipe';

describe('FormatoInscripcionesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoInscripcionesPipe();
    expect(pipe).toBeTruthy();
  });
});
